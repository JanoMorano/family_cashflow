version: '3.8'

services:
  family_cashflow:
    build: ./backend
    ports:
      - "3000:3000"       # http://localhost:3000
    volumes:
      - budget_data:/data  # data persists across restarts
    restart: unless-stopped

volumes:
  budget_data:
